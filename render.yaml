services:
  - type: web
    name: airlink-relay
    env: docker
    dockerfilePath: Dockerfile
    plan: free
    region: oregon
    healthCheckPath: /health
    autoDeploy: true
    envVars:
      - key: PORT
        value: 3001
      - key: TRUST_PROXY
        value: "true"
